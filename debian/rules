#!/usr/bin/make -f

# Enable additional explot mitigation
# (PIE and *full* RELRO at the time of this writing)
export DEB_BUILD_MAINT_OPTIONS=hardening=+all

%:
	dh $@ 

override_dh_missing:
	dh_missing --fail-missing --list-missing

override_dh_strip:
	dh_strip --dbg-package=maya-calendar-dbg
